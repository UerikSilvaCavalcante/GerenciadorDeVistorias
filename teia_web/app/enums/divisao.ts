export enum TipoAreaServico {
  Interna = 1,
  Externa = 2,
}

export enum TipoBanheiro {
  Social = 1,
  Privado = 2,
  Empregada = 3,
}

export enum TipoGaragem {
  Coberta = 1,
  Descoberta = 2,
}
